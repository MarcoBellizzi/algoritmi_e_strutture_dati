package strutture;

public class BackTracking {

	/*
	 * List sol = {}
	 * boolean solve(sol) {
	 * 		x = MIN_VAL;
	 * 		while(x < MAX_VAL) {
	 * 
	 * 									for tutti le stazione, se non sono spente
	 * 
	 * 			if(canAdd(x,sol) {     contiene ciclo
	 * 				add(x,sol);          spegni la stazione x
	 * 				if(isComplete(sol)) {    
	 * 					return true;
	 * 				}
	 * 				else {
	 * 					if(solve(sol)) {  // prossima posizione della lista
	 * 						return true;
	 * 					}
	 * 					else {
	 * 						remove(x,sol);
	 * 						x = next(x);
	 * 					}
	 * 				}
	 * 			}
	 * 			else {
	 * 				x = next(x);
	 * 			}
	 * 		}
	 * 		return false;
	 * 	}
	 * 
	 */
	
}
